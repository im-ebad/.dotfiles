# Enable vsync to prevent tearing
vsync = true;

# Backend and performance tweaks
backend = "glx";
glx-no-stencil = true;
glx-no-rebind-pixmap = true;
glx-swap-method = "buffer-age";

# Fading and transparency (optional)
fading = true;
fade-delta = 5;
shadow = true;
shadow-radius = 12;
shadow-offset-x = -15;
shadow-offset-y = -15;

# Optional: Fix for Intel tearing
unredir-if-possible = false;
